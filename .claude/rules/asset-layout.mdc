---
description: Asset and diagram layout for slash-space
alwaysApply: true
---

# Asset Layout

- 流程图文件统一放在 `x/`：`.canvas`、`.excalidraw`、`.excalidraw.md`
- `raw/articles/foo.md` 的附件统一放在 `raw/assets/articles/foo/`
- 仅出现在 `wiki/concepts/...` 的图片，统一放在 `raw/assets/wiki/concepts/<topic>/<页面名>/`
- `wiki/` 不新增独立附件目录，只引用 `raw/assets/` 与 `x/` 中的资源
- 修改历史引用时，只改目标文件真实存在且归属明确的路径；不要猜测性重写坏链
